{
	"$schema": "https://turborepo.dev/schema.json",
	"tasks": {
		"build": {
			"dependsOn": ["prebuild", "^build"],
			"outputs": ["dist/**"]
		},
		"prebuild": {},
		"check-types": {},
		"dev": {
			"dependsOn": ["prebuild", "build"]
		},
		"//#lint": {},
		"//#lint:fix": {
			"cache": false
		}
	}
}
